// Rolls back the most recent migration. drizzle-kit does not roll back by default;
// each migration ships a paired down-SQL (SF-DOC-05 discipline) applied here.
// Implemented with migration 001 (SF-003).
console.log('db:rollback — not yet implemented (arrives with migration 001, SF-003).');

export {};
