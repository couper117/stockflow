# Root-level env consumed by docker-compose.yml (Postgres service).
# Copy to .env:  cp .env.example .env
# The backend has its own /backend/.env.example — keep DB_* values in sync.
DB_DATABASE=stockflow
DB_USERNAME=stockflow
DB_PASSWORD=secret
DB_PORT=5432
